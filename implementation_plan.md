# 🚀 2.5D 청능 훈련 피아노 앱 마스터 구현 계획 (Master Plan)

본 문서는 프로젝트(`rn-riski25d`)의 초반 성능 최적화 철학부터 시작하여, 52음 M4A 확장, 옥타브 뷰포트 시프트, 그리고 지능형 자동 포커싱 시스템에 이르기까지 진행된 모든 설계 원칙과 개발 이정표를 시간 순서대로 통합 정리한 단일 마스터 계획서입니다.

---

## 📐 핵심 기술 아키텍처 및 최적화 원칙 (이전 1편 연동)

1. **60FPS 보장을 위한 UI 스레드 바이패스:**
   - React Native의 `useState` 상태 변경으로 화면 전체를 리렌더링(Re-render)하는 방식은 다중 터치나 부드러운 조작 시 극심한 프레임 드랍을 유발합니다.
   - 이를 극복하기 위해 터치에 따른 Y축 이동 및 그림자 변화 등은 Reanimated의 `SharedValue`와 `useAnimatedStyle`을 활용하여 **UI 스레드에서 직접 60fps 이상의 애니메이션으로 구동**되도록 설계합니다.
2. **개별 건반 컴포넌트 최적화:**
   - 개별 건반을 `PianoKey` 컴포넌트로 모듈화하여 `React.memo`로 감싸고, 각각 독립적인 애니메이션 및 `onPressIn` / `onPressOut` 이벤트를 처리하도록 구현하여 불필요한 전체 렌더링을 방지합니다.

---

## 📅 단계별 구현 타임라인 (Chronological Phases)

### 🌌 Phase 1: 2.5D 기반 3단 레이어 구축 (완료)
안드로이드 GPU 가속을 100% 활용하는 화려한 2.5D 공간 레이어 원칙을 정의했습니다.
1. **[Layer 1 - Background]:** Skia 기반 우주 은하 배경
2. **[Layer 2 - Midground]:** 인터랙션 건반 및 터치 물결 레이어
3. **[Layer 3 - Foreground]:** 반투명 점수/미션 유리 패널
- 세 레이어는 기기의 자이로 센서 기울기에 맞춰 서로 다른 속도로 움직여 입체감을 주는 Parallax 효과를 적용합니다.

### 🎨 Phase 2: 시각 디자인 및 컴포넌트 모듈화 (완료)
- **`SpaceBackground.tsx`:** 어두운 보랏빛/청록빛 네온 우주 캔버스.
- **`TrainingPanelLab.tsx`:** 사이버펑크 민트색 강조 폰트 및 Glassmorphism 반투명 유리 카드.
- **`RippleLayer.tsx`:** 터치 좌표를 추적해 Skia로 동심원이 퍼져나가는 고속 애니메이션 레이어.

### 🎹 Phase 3: 가로 모드 및 2.5D 피아노 건반 시각화 (완료)
- 화면을 가로(Landscape)로 고정하고, 좌측 240px 공간은 유리 미션 제어반이 차지하며, 우측의 모든 잔여 영역은 2.5D 피아노 건반이 꽉 채우도록 구조화했습니다.
- 백건 14개와 흑건 10개를 Reanimated 기반의 `AnimatedPressable` 컴포넌트와 입체 섀도우 스타일을 활용하여 마감이 둥글고 정교한 형태로 배치해 렌더링했습니다.

### 🎼 Phase 4: 52음 M4A 확장 및 옥타브 시프트/자동 포커싱 통합 (완료)
- **52음 M4A 포팅 및 저지연 재생:**
  - 사용자가 제공한 52개 고음질 `.m4a` 에셋(`assets/m4a-sounds/`)을 Metro 번들러에 정적 매핑하여 사운드 누락 현상을 없앴습니다.
  - `expo-audio`의 `createAudioPlayer`를 활용해 프리로드를 우회하고 지연 없는 재생 라이프사이클을 가동했습니다.
- **옥타브 뷰포트 시프트 (Octave Shift):**
  - 화면에는 백건 14개(2옥타브 범위)만 큼직하게 렌더링하고, 옥타브 버튼 조작으로 1옥타브씩 화면이 좌우로 슬라이딩되는 시프트 메커니즘을 완성했습니다.
- **9. 자동 옥타브 포커싱 (Auto-Focus Viewport):**
  - 신규 문제가 재생될 때 정답 건반이 화면 밖에 있으면, `useAutoFocusViewport` 훅이 이를 감지하여 정답 음이 화면 중앙 영역에 보이도록 뷰포트 시작 인덱스(`viewportStartIdx`)를 안전하게 자동 갱신해 줍니다.
  - 자동 갱신은 오직 `currentNote` 이벤트에만 의존하며, 현재 뷰포트는 `startRef`로 관리하므로 수동 옥타브 이동과 서로 락(Lock)이 걸려 충돌하지 않도록 예외 처리했습니다.
- **Rive 런타임 오류 방지:**
  - Expo Go 환경 구동 시 네이티브 패키지 미지원으로 발생하는 크래시를 방지하기 위해 [MusicScreen.tsx](file:///d:/Projects/rn-riski25d/screens/MusicScreen.tsx) 내의 Rive 렌더러를 일시적으로 주석 처리해 비활성화했습니다.

---

### 📳 Phase 5: 2.5D 타격감 및 Haptics 진동 적용 (완료)
- **햅틱(Haptic) 피드백 연동:** `expo-haptics` 라이브러리를 추가하여, 건반 터치 시 가벼운 물리적 진동 효과를 발생시켜 실제 피아노를 누르는 듯한 타격감을 제공합니다.
- **2.5D Y축 프레스 애니메이션:** `useSharedValue`를 활용하여 [MusicScreen.tsx](file:///d:/Projects/rn-riski25d/screens/MusicScreen.tsx) 내의 개별 건반 UI 스레드 상에서 백건 14px, 흑건 18px이 하강하는 눌림 모션과 그림자 투명도 조절을 60fps로 구현 완료했습니다.

---

## 🔮 Phase 6: 지능형 내비게이션 및 인터랙티브 네온 비주얼 (Next Step)

1. **🗺️ 피아노 미니 가이드 맵 (Piano Minimap):**
   - 메인 건반 상단에 52음 전체의 축소형 미니맵 띠를 그리고, 현재 확대 노출된 2옥타브 범위를 하이라이팅 박스로 투명 표시합니다.
   - 사용자가 미니맵의 특정 구역을 터치하면 메인 건반의 뷰포트가 즉각 텔레포트 이동하는 옥타브 네비게이터를 구현합니다.
2. **💫 음의 비행 (Sound Trail) 웨이브 비주얼라이저:**
   - 건반을 칠 때마다 Skia Canvas에 네온 빛의 은하수 입자들이 수직으로 뿜어 올라고 소리의 감쇄 곡선에 따라 페이드아웃되는 입자 궤적 비주얼라이저를 결합합니다.
3. **🎨 음역대별 엠비언트 테마 체인저 (Color-to-Pitch Mapping):**
   - 뷰포트가 저음(C1 대역)일 때는 딥 블루, 중음(C3 대역)일 때는 퍼플, 고음(C5 대역)일 때는 네온 핑크로 배경 번짐(Glow) 효과를 그라데이션 보간 연출합니다.
4. **🌌 소리 별자리 (Sound Constellation) 청능 게임화:**
   - 훈련 연속 정답 콤보 시 우주 배경에 별들이 생성되고 네온 라인으로 별자리가 이어지며, 오답 시 우주 먼지로 흩어지는 비주얼 게임 루프를 추가합니다.

---

## 📌 Rive 통합 계획 (보류 / Pending)
- 캐릭터 및 복잡한 컴포넌트의 Rive 애니메이션 모듈 통합은 Phase 5의 오토 포커싱 및 엠비언트 비주얼이 완전히 안착한 후 진행하도록 보류 결정되었습니다.
