# 📋 외부 AI 질문용 프롬프트 템플릿 (Copy & Paste용)

이 문서는 다른 AI 모델(예: ChatGPT, Claude 등)에 복사하여 붙여넣음으로써, 2.5D 청능 피아노 앱의 UI/UX 개선안에 대한 고퀄리티 코드를 얻어내기 위해 기획된 **정교한 질문 프롬프트 모음**입니다. 

필요한 질문 템플릿 블록을 그대로 복사해서 사용하세요.

---

## 🛠️ [기본 컨텍스트] 질문 시 항상 먼저 제공할 기본 정보
> **Tip:** 아래의 시스템 기본 정보를 먼저 제공한 뒤, 개별 질문을 덧붙이면 AI가 완벽한 맥락 하에 답변을 작성합니다.
```markdown
[시스템 기본 정보]
- 기술 스택: React Native (Expo SDK 57)
- 핵심 라이브러리: @shopify/react-native-skia, react-native-reanimated, expo-audio
- 앱 목적: 가로 모드(Landscape) 전용 피아노 청능(聽能) 훈련 앱
- 현재 스펙: C1 ~ D#5 (52음계)의 M4A 음원을 사전 로드하고 있으며, 화면에는 한 번에 14개 백건(2옥타브 영역)의 뷰포트만 옥타브 시프트(Octave Shift)하여 확대해서 보여주는 상태입니다.
```

---

## 🎹 Part 1. 핵심 UI/UX 개선 요소 질문 프롬프트

### 질문 1: 건반의 2.5D 물리적 타격감 및 햅틱 프레스 효과 구현
```markdown
[질문]
현재 React Native Expo 앱에서 Skia와 Reanimated를 사용하여 2.5D 피아노 건반을 렌더링하고 있습니다. 건반을 누를 때 단색 반전이 아니라, 실제 물리적 건반이 아래로 들어가는 듯한 햅틱 타격감을 구현하고 싶습니다.

다음 세부 조건에 맞게 백건과 흑건 컴포넌트 코드를 작성해 주세요:
1. 백건은 누를 때 Y축으로 약 10px 내려앉으며 입체 그림자(Shadow)가 사라지거나 옅어져야 하고, 흑건은 Y축으로 약 8px 내려앉아야 합니다.
2. 손가락을 뗄 때 Reanimated의 `withTiming`을 사용하여 부드럽게 원래 위치로 복원되는 물리 모션을 구현해 주세요.
3. 60fps 이상으로 매끄럽게 동작하도록 UI 스레드 연산을 타겟팅해 주세요.
```

### 질문 2: 어두운 성운 테마의 네온 그라데이션 배경 구현
```markdown
[질문]
피아노 청능 훈련 앱의 배경 테마를 개선하고 싶습니다. 단순 어두운 무채색 배경(#333) 대신, 우주의 어두운 보랏빛과 청록빛 성운이 뒤섞인 듯한 몽환적인 네온 그라데이션 배경을 Skia를 활용해 구현하고 싶습니다.

다음 사양을 충족하는 배경 컴포넌트 코드를 작성해 주세요:
1. @shopify/react-native-skia의 Canvas, LinearGradient, RadialGradient 등을 사용하여 신비로운 네온 우주 광원을 연출해 주세요.
2. 성운의 광원 색상들이 아주 미세하고 부드럽게 일렁이며 변하는 셰이더 애니메이션 효과를 구현해 주세요.
```

### 질문 3: 반투명 유리 제어판(Glassmorphic Control Panel) 스타일링
```markdown
[질문]
React Native에서 난이도 및 점수를 보여주는 좌측의 제어반 패널을 미래지향적인 사이버펑크 스타일의 Glassmorphism(유리 효과) 스타일로 개선하고자 합니다.

다음 사양에 맞추어 스타일 및 컴포넌트 구조를 설계해 주세요:
1. 반투명한 블러 느낌(투명도와 미세한 노이즈/텍스처)과 유리 테두리를 흐르는 미세한 흰색 네온 광선(Border Glow) 효과를 구현해 주세요.
2. 점수 및 피드백 텍스트는 선명한 민트색 및 네온 그린의 폰트로 포인트를 주어 시인성을 극대화해 주세요.
```

### 질문 4: 옥타브 시프트 전환 시 좌우 슬라이딩 애니메이션 연출
```markdown
[질문]
현재 52음계 중 백건 14개만 뷰포트로 지정해 화면에 보여주고 있으며, '옥타브 낮춤/높임' 버튼을 통해 뷰포트를 이동시키고 있습니다. 이동할 때 건반들이 뚝뚝 끊기며 바뀌는 것이 아니라, 화면에서 좌우로 미끄러지며(Swipe Transition) 전환되는 연주 연출을 구현하고 싶습니다.

1. Reanimated의 `useSharedValue`와 `useAnimatedStyle`을 활용하여 옥타브 변경 시 건반 뷰포트 레이어가 좌/우로 휙 미끄러지는 모션 효과를 렌더링하는 코드를 구현해 주세요.
```

### 질문 5: 터치 피드백 물결(Ripple Effect) 잔상 효과 연동
```markdown
[질문]
피아노 건반을 터치하는 순간, 터치 좌표(x, y)를 기준으로 네온 컬러의 원형 물결이 자연스럽게 퍼지다가 서서히 사라지는(Ripple Effect) 효과를 Skia 캔버스 레이어로 얹어주고 싶습니다.

1. 다중 터치(화음) 상황을 지원하여 여러 손가락이 닿을 때 각각 독립적인 Ripple이 발생해야 합니다.
2. Reanimated의 sequence 애니메이션을 사용해 물결 반경은 부드럽게 커지고 투명도(opacity)는 1에서 0으로 페이드아웃 되게 설계해 주세요.
```

---

## 🌌 Part 2. 독창적이고 인터랙티브한 비주얼 & 게임화 아이디어 질문 프롬프트

### 질문 6: "소리 별자리(Sound Constellation)" 청능 게임화 로직
```markdown
[질문]
청능 훈련 앱에서 사용자가 정답 음계를 맞출 때마다 우주 배경 화면에 가상의 별들이 생기고, 연속 정답 콤보를 달성하면 별들이 네온 광선 선(Beam Line)으로 서로 연결되며 별자리를 형성해가는 재미있는 게임 요소를 구현하고 싶습니다.

1. 정답 횟수에 따라 별의 좌표 배열 상태를 관리하고, Skia를 사용하여 이 좌표들을 잇는 부드러운 발광선(Glow Line)을 렌더링하는 코드를 짜주세요.
2. 오답 시 별자리가 미세하게 먼지로 흩어지며 초기화되는 비주얼 효과도 곁들여 주세요.
```

### 질문 7: 건반 타격 시 솟구쳐 오르는 "음의 비행(Sound Trail)" 입자 비주얼라이저
```markdown
[질문]
피아노 건반을 타격할 때, 해당 건반 위치에서 수직으로 은하수 형태의 네온 입자 트레일(Particle Trail)이 하늘로 솟구쳤다가 흩어지는 비주얼라이저를 구현하고 싶습니다.

1. 건반 타격 시 입자 인스턴스를 발생시키고, 이 입자들이 Y축 위 방향 및 무작위 X축 확산 방향으로 물리 연산(속도, 중력 감쇠)에 따라 움직이게 해주세요.
2. 오디오가 재생되는 듀레이션(Duration)이나 디케이(Decay) 시간에 부합하여 입자들이 점차 소멸하는 효과를 Skia Canvas 루프 내에 구현해 주세요.
```

### 질문 8: 옥타브 범위에 따른 "음역대별 엠비언트 테마 체인저(Color-to-Pitch Mapping)"
```markdown
[질문]
52음의 범위(저음/중음/고음)에 맞춰 화면 전체의 번짐(Glow) 분위기 색상이 자동으로 바뀌는 감성적 효과를 구현하고 싶습니다.

1. 현재 뷰포트 범위가 낮은 옥타브(C1 대역)일 때는 딥 블루 테마, 중간 옥타브(C3 대역)일 때는 바이올렛 퍼플, 높은 옥타브(C5 대역)일 때는 마젠타 핑크로 설정합니다.
2. 옥타브 시프트 버튼을 누르면 배경 엠비언트 그라데이션이 새로운 색상으로 1~2초간 부드럽게 보간(Color Interpolation)되며 스며드는 Reanimated/Skia 애니메이션 코드를 작성해 주세요.
```

### 질문 9: 자이로센서 연동 2.5D 시차 Parallax 공간 레이아웃 구현
```markdown
[질문]
Expo의 `expo-sensors` 내 `DeviceMotion` 또는 `Gyroscope`를 이용하여 기기를 기울일 때 2.5D 공간감을 주는 Parallax 레이아웃을 구현하고 싶습니다.

1. 기울기 각도에 따라 배경(성운), 중경(피아노 건반), 전경(제어 패널)이 각기 다른 가중치(오프셋 비율)와 약간의 시간 차를 갖고 반대 방향으로 미세하게 움직이는 구조를 설계해 주세요.
2. 센서 데이터 수신 시 UI 스레드 렌더링 락이 걸리지 않도록 Reanimated SharedValue를 사용하여 보간하는 최적의 코드를 제안해 주세요.
```

### 질문 10: 물결 터치 궤적(Ripple Glissando) 및 롱프레스 오라(Aura) 효과
```markdown
[질문]
피아노 건반을 손가락으로 주르륵 쓸어내리는 글리산도 연주시 터치 드래그 궤적을 실시간으로 추적하여 네온 빛의 물결 잔상이 부드럽게 흘러가도록 구현하고 싶습니다.

1. 터치 드래그 경로(Path)를 수집하여 터치 이동 속도에 비례해 물결처럼 서서히 흔들리며 지워지는 Skia 연동 코드를 구현해 주세요.
2. 특정 건반을 손가락을 떼지 않고 꾹 누르고 있을 때(Long Press), 해당 건반 바운더리를 따라 아우라 파형이 일렁이며 호흡하는 듯한 셰이더 효과도 구현해 주세요.
```
