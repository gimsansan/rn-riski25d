# 🚀 2.5D 피아노 앱 전체 개발 기록 (Master Walkthrough)

본 문서는 프로젝트(`rn-riski25d`)에서 진행된 기능 개발 및 시각적 변화들을 단계별로 기록한 총괄 마스터 워크스루(Walkthrough)입니다. 이전의 모든 세부 내용과 테스트 방법까지 하나도 빠짐없이 포함되어 있습니다.

---

# 🎨 Phase 2: 2.5D 시각 디자인 및 컴포넌트 구조화 완료

약속드린 대로 3가지 시각 기능(우주 배경, 우측 점수 패널, 터치 이펙트)을 각각 독립된 파일로 깔끔하게 분리하고 `App.tsx`에 연동하는 작업을 모두 마쳤습니다! 

## ✨ 새롭게 만들어진 파일들

1. **[NEW] [SpaceBackground.tsx](file:///d:/Projects/rn-riski25d/components/SpaceBackground.tsx)**
   - `@shopify/react-native-skia`의 `Canvas`와 `LinearGradient`를 활용하여 어두운 보랏빛/청록빛 우주 느낌의 배경을 구현했습니다.
   - 메인 화면의 제일 뒤쪽(Layer 1)에 배치됩니다.

2. **[NEW] [TrainingPanelLab.tsx](file:///d:/Projects/rn-riski25d/components/TrainingPanelLab.tsx)**
   - 반투명한 유리창(Glassmorphism) 느낌의 세련된 디자인으로 구현되었습니다.
   - 점수(Score)가 사이버펑크 틱한 민트색 폰트로 강조되어 있으며, 우측 중앙에 떠 있도록 디자인했습니다.
   - 메인 화면의 제일 앞쪽(Layer 3)에 배치되며, 스마트폰을 기울일 때 둥둥 떠서 움직입니다.

3. **[NEW] [RippleLayer.tsx](file:///d:/Projects/rn-riski25d/components/RippleLayer.tsx)**
   - 화면 중앙 빈 공간(중경, Layer 2)을 터치할 때 발생하는 터치 좌표(x,y)를 실시간으로 받아옵니다.
   - Reanimated의 `withTiming` 엔진을 이용해 터치한 곳에서부터 Skia로 그린 원형 물결이 자연스럽게 커지면서 사라지는 이펙트를 완성했습니다.

## ⚙️ App.tsx 의 변화

- **[MODIFY] [App.tsx](file:///d:/Projects/rn-riski25d/App.tsx)**
  - 길었던 3계층 레이어 코드가 위 3개의 컴포넌트(`<SpaceBackground />`, `<TrainingPanelLab />`, `<RippleLayer />`)를 불러오는 형식으로 아주 짧고 직관적이게 리팩토링 되었습니다.
  - 전 화면을 덮는 `<Pressable>` 이벤트를 달아서 터치 좌표를 추출하는 로직이 추가되었습니다.

> [!TIP]
> **테스트 방법:**
> 지금 켜져 있는 에뮬레이터에서 화면이 새로고침 되었을 것입니다. 
> 화면의 빈 공간(가운데 영역)을 여러 군데 콕콕 터치해 보세요! 터치한 곳마다 영롱한 색상의 원형 물결(Ripple) 퍼져나가는 것을 보실 수 있습니다.

---

# 🎹 Phase 3: 가로 모드 Skia 2.5D 피아노 건반 구현 완료

사용자님의 훌륭한 제안대로, Rive 대신 Skia와 Reanimated를 활용해 '가로 모드 전용 2.5D 피아노 앱'의 시각적 뼈대를 완성했습니다!

## ✨ 작업 요약

1. **[NEW] [PianoKeyboard.tsx](file:///d:/Projects/rn-riski25d/components/PianoKeyboard.tsx) 생성**
   - `@shopify/react-native-skia`의 `RoundedRect`, `LinearGradient`, `Shadow`를 적극 활용하여, 이미지가 아닌 100% 코드로 그려진 **고해상도 2.5D 피아노 건반**을 만들었습니다.
   - 하얀 건반(백건) 14개(2옥타브)와 검은 건반(흑건) 10개를 정확한 수학적 비율로 계산하여 배치했습니다.
   - 건반 아래쪽에 실감 나는 그림자(Shadow)를 넣어 입체감을 극대화했습니다.

2. **[MODIFY] [App.tsx](file:///d:/Projects/rn-riski25d/App.tsx) 레이아웃 전면 수정**
   - 이제 앱을 켜면 화면이 무조건 **가로(Landscape) 모드**로 고정됩니다.
   - 화면을 좌우로 분할하여, **좌측엔 점수/훈련 패널**이 뜨고, **우측 빈 공간엔 거대한 피아노 건반**이 꽉 차게 들어오도록 배치했습니다.

> [!TIP]
> **테스트 방법:**
> 1. 지금 터미널 창을 한 번 클릭하고 `r` 키를 눌러 에뮬레이터 화면을 새로고침해 주세요.
> 2. 화면이 가로로 눕혀지면서, 왼쪽에는 기존 패널이, 오른쪽에는 눈부시게 예쁜 피아노 건반 레이어가 렌더링된 것을 보실 수 있습니다.
> 3. 아직 피아노 건반을 눌렀을 때 소리가 나거나 건반이 들어가는 애니메이션은 넣지 않았습니다. (외형 뼈대만 완성된 상태입니다.)

---

# 🎹 Phase 4: 다중 터치(화음) 및 물리 애니메이션 구현 완료!

진짜 피아노처럼 여러 손가락으로 동시에 누르거나, 손가락을 떼지 않고 주르륵 훑는(글리산도) 고급 터치 제스처와 시각적 물리 효과가 추가되었습니다.

## ✨ 새롭게 구현된 마법들

1. **완벽한 다중 터치(Multi-Touch) 로직**
   - 손가락 10개를 동시에 올려도 모두 개별적으로 인식됩니다. (화음 연주 가능)
   - `onTouchMove`와 실시간 좌표(x,y) 계산 로직을 결합하여, 화면에서 손을 떼지 않고 건반 위를 쭉 훑고 지나가면 부드럽게 인접한 건반들이 차례대로 눌립니다. (글리산도 기법)
   - 손가락의 Y 좌표가 건반 상단 60% 이내면 흑건을 우선 판정하고, 아니면 백건을 판정하도록 정밀한 수학적 바운딩 박스(Bounding Box)를 짰습니다.

2. **2.5D 햅틱 타격감 (누름 시각 효과)**
   - **백건:** 누르는 순간 건반이 10px 밑으로 꺼지면서 그림자가 사라지고 색상이 약간 짙어집니다.
   - **흑건:** 누르는 순간 8px 밑으로 꺼지며 그림자가 사라지고 더 어두워집니다.
   - 이 모든 효과는 Skia의 `RoundedRect` 렌더링 루프를 통해 모바일 기기에서 초당 60~120 프레임의 놀라운 부드러움으로 작동합니다.

> [!TIP]
> **테스트 방법:**
> 지금 켜져 있는 에뮬레이터에서 키보드 `r` 키를 눌러 새로고침해 주세요.
> 1. 여러 손가락(혹은 마우스 클릭)으로 피아노 건반을 꾹꾹 눌러보세요! 건반이 물리적으로 '쑥' 하고 들어가는 2.5D 효과를 볼 수 있습니다.
> 2. 마우스를 꾹 누른 채로 양옆으로 빠르게 문질러 보세요. 손가락이 지나가는 자리의 건반들이 촤라락 반응할 것입니다!

---

# 🎹 Phase 4-3: 사운드 연동 및 시연 완료!

무거운 용량이나 라이선스 문제 없이 프로토타이핑하기 가장 완벽한 **초경량 어쿠스틱 피아노 샘플(.mp3)** 들을 다운로드하여 앱에 완벽하게 연동했습니다!

## ✨ 새롭게 구현된 마법들

1. **[NEW] 사운드 파일 24개 다운로드 (`assets/sounds/`)**
   - 2옥타브(C4 ~ B5)에 해당하는 24개의 건반 소리 `.mp3` 파일을 다운로드하는 임시 Node.js 스크립트를 작성하고 실행했습니다. 파일 크기가 매우 작아 모바일에 최적화되어 있습니다.

2. **[NEW] `SoundManager.ts` 팩토리 클래스 제작**
   - 사용자가 피아노를 치려고 할 때 로딩(버퍼링)이 생기지 않도록, 앱이 켜지자마자 24개의 사운드 객체를 **메모리에 0.1초 만에 프리로드(Preload)** 하는 훌륭한 매니저 클래스를 만들었습니다.
   - 이미 재생 중인 소리를 다시 눌렀을 때, 소리가 겹치지 않고 즉각적으로 타격음이 다시 재생되도록 `setPositionAsync(0)` 기법을 적용해 레이턴시를 없앴습니다.

3. **[MODIFY] `PianoKeyboard.tsx` 사운드 방아쇠(Trigger)**
   - 아까 만들어둔 다중 터치 로직에 딱 2줄의 코드를 추가했습니다.
   - 손가락이 특정 건반 영역에 진입하여 '새로운 건반'으로 인식되는 그 찰나의 순간에, `SoundManager`에게 해당 건반 번호(0~23)의 소리를 내달라고 요청합니다.

> [!TIP]
> **테스트 방법:**
> 1. 지금 터미널 창을 한 일 클릭하고 `r` 키를 눌러 에뮬레이터 화면을 새로고침해 주세요.
> 2. 볼륨을 켜시고 피아노 건반을 아무거나 꾹 눌러보세요!
> 3. 손가락을 떼지 않고 좌우로 비비며(글리산도) 아름다운 하프 소리(?)를 경험해 보세요!
