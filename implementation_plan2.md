# 🚀 M4A 52음 확장 및 옥타브 시프트 뷰포트 구현 계획

본 계획은 사용자가 제공한 52개의 고음질 `.m4a` 오디오 에셋을 기반으로 청능 훈련 피아노의 음역대를 확장하고, 옥타브 전환(Octave Shift) 및 자동 옥타브 포커싱 기능을 구현하기 위한 설계도입니다.

---

## User Review Required

> [!IMPORTANT]
> **1. 기존 MP3 파일 유지 및 신규 M4A 디렉토리 준비 (완료)**
> - 기존 `assets/sounds/note_*.mp3` 파일들은 그대로 보존합니다.
> - 사용자께서 제공해 주신 52개의 `.m4a` 음원은 `assets/m4a-sounds/` 디렉토리에 정상 수용 완료했습니다.
> 
> **2. 옥타브 시프트 및 자동 옥타브 포커싱 도입 (완료)**
> - 한 번에 **14개의 백건(2옥타브)** 범위만 뷰포트에 확대 렌더링하고, 옥타브 버튼 조작으로 슬라이딩되도록 구현했습니다.
> - 새 문제가 출제될 때 정답 건반 위치로 뷰포트를 자동 순간이동시키는 `useAutoFocusViewport` 훅을 통합했습니다.
> 
> **3. Rive 런타임 크래시 방지 조치 (완료)**
> - Expo Go 환경 구동 시 네이티브 패키지 미지원으로 인한 크래시를 방지하기 위해 Rive 렌더러를 일시적으로 주석 처리해 비활성화했습니다.

---

## Proposed Changes

### [Audio Assets]

#### [NEW] `assets/sounds/piano_m4a/` 디렉토리 생성
- 사용자 제공 `.m4a` 음원(52개: C1 ~ D#5)이 담길 신규 전용 폴더입니다.

---

### [Main Component]

#### [MODIFY] [existing_file/index.tsx](file:///d:/Projects/rn-riski25d/existing_file/index.tsx)
- **52음 M4A 매핑 테이블 추가:**
  - `soundFiles` 객체에 `C1` ~ `D#5` 52개 음의 `require('../assets/m4a-sounds/C1.m4a')` 경로를 정적으로 선언했습니다.
- **옥타브 뷰포트 상태 추가:**
  - `viewportStartIdx` 상태 변수를 도입하여 현재 화면에 보여줄 시작 음의 백건 인덱스(기본값 14: C3)를 관리하며, 화면에는 14개의 백건만 잘라 렌더링합니다.
- **자동 옥타브 포커싱 통합:**
  - 새 문제가 재생될 때 정답이 화면 밖에 있으면 뷰포트 시작 인덱스를 해당 영역으로 즉시 갱신해 주는 `useAutoFocusViewport` 장치를 추가했습니다.
- **App.tsx 엔트리 교체:**
  - 앱 구동 시 곧장 신규 52음 옥타브 시프트 피아노 페이지(`MugicPage`)가 로드되도록 엔트리 타겟을 스위칭했습니다.

---

## Verification Plan

### Manual Verification
1. `assets/sounds/piano_m4a/` 디렉토리에 52개 음원이 정상적으로 복사/제공되었는지 확인합니다.
2. `npx tsc --noEmit`를 실행하여 컴파일 및 빌드 오류가 없는지 최종 확인합니다.
3. 앱 구동 후 피아노 건반이 큼직하게 렌더링되는지 확인하고, 옥타브 이동 버튼 클릭 시 화면의 건반 음계가 부드럽게 시프트되는지 터치 동작을 테스트합니다.
