# 🚀 M4A 52음 확장 및 옥타브 시프트 뷰포트 구현 계획

본 계획은 사용자가 제공할 52개의 고음질 `.m4a` 오디오 에셋을 기반으로 청능 훈련 피아노의 음역대를 확장하고, 모바일 화면의 가독성을 높이기 위해 한 번에 특정 옥타브 영역만 확대하여 보여주는 **"옥타브 전환(Octave Shift)"** 기능을 구현하기 위한 설계도입니다.

---

## User Review Required

> [!IMPORTANT]
> **1. 기존 MP3 파일 유지 및 신규 M4A 디렉토리 준비**
> - 기존에 다운로드받았던 `assets/sounds/note_*.mp3` 파일들은 전혀 삭제하지 않고 보존합니다.
> - 사용자께서 직접 제공해 주실 52개의 `.m4a` 음원을 담기 위한 디렉토리인 `assets/sounds/piano_m4a/` 폴더를 새로 생성하여 준비하겠습니다.
> 
> **2. Metro 번들러를 위한 52음 정적 매핑 작성**
> - React Native Metro 번들러는 동적 임포트(`require(variable)`)가 불가능하므로, 52개의 `C1.m4a` ~ `D#5.m4a` 에셋 경로를 명시하는 매핑 객체를 코드 내에 고정 정의합니다.
> 
> **3. 옥타브 시프트(Octave Shift) 뷰포트 시스템 도입**
> - 52개의 건반을 한 번에 다 보여주면 건반 폭이 좁아져 터치가 어렵습니다.
> - 한 번에 **15개의 백건(약 2옥타브)** 범위만 뷰포트(Viewport)에 꽉 차게 렌더링하고, 상단에 `[ ◀ 옥타브 낮춤 ]`, `[ 옥타브 높임 ▶ ]` 버튼을 제공하여 동적으로 보여지는 영역을 슬라이딩 시키겠습니다.

---

## Proposed Changes

### [Audio Assets]

#### [NEW] `assets/sounds/piano_m4a/` 디렉토리 생성
- 사용자 제공 `.m4a` 음원(52개: C1 ~ D#5)이 담길 신규 전용 폴더입니다.

---

### [Main Component]

#### [MODIFY] [existing_file/index.tsx](file:///d:/Projects/rn-riski25d/existing_file/index.tsx)
- **52음 M4A 매핑 테이블 추가:**
  - `soundFiles` 객체에 `C1` ~ `D#5` 52개 음의 `require('../assets/sounds/piano_m4a/C1.m4a')` 경로를 정적으로 선언합니다.
- **옥타브 뷰포트 상태 추가:**
  - `currentViewportStart` 상태 변수를 도입하여 현재 화면에 보여줄 시작 음의 인덱스(기본값은 가청도가 좋은 중간 옥타브 `C3`)를 관리합니다.
  - 화면에는 이 뷰포트 시작점부터 백건 15개 범위에 해당하는 음들만 필터링하여 동적으로 크게 그려줍니다.
- **옥타브 이동 컨트롤러 추가:**
  - 피아노 화면 상단에 현재 범위(예: `C3 ~ D5`)를 보여주는 라벨과 좌/우 옥타브 이동 단축 버튼을 제공합니다.
- **SoundManager 연동 강화:**
  - 52개 음원 전체를 `SoundManager.ts`를 활용해 재생하거나, `existing_file/index.tsx` 내에서 로딩 부담을 줄이기 위해 사용자가 짚은 옥타브 근처 음들만 부분 캐싱하여 효율적으로 로드하는 구조를 유지합니다.

---

## Verification Plan

### Manual Verification
1. `assets/sounds/piano_m4a/` 디렉토리에 52개 음원이 정상적으로 복사/제공되었는지 확인합니다.
2. `npx tsc --noEmit`를 실행하여 컴파일 및 빌드 오류가 없는지 최종 확인합니다.
3. 앱 구동 후 피아노 건반이 큼직하게 렌더링되는지 확인하고, 옥타브 이동 버튼 클릭 시 화면의 건반 음계가 부드럽게 시프트되는지 터치 동작을 테스트합니다.
